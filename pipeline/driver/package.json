{
  "name": "iron-hammer-driver",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "铁锤小队 pipeline ① Loop 引擎:事件触发 + claude -p 循环驱动",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "gate": "npm run lint && npm run typecheck && npm run test",
    "mutation": "stryker run",
    "drive": "tsx src/loop.ts",
    "drive-parallel": "tsx src/drive-parallel.ts",
    "enqueue": "tsx src/bin-enqueue.ts",
    "enqueue-sqlite": "tsx src/bin-enqueue-sqlite.ts",
    "replay": "tsx src/bin-replay.ts"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@types/node": "^22.10.0",
    "eslint": "^9.17.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.18.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
