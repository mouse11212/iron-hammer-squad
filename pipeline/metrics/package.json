{
  "name": "iron-hammer-metrics",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "铁锤小队 pipeline ② 可观测:harness 四指标 + 追溯链 + 看板",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "gate": "npm run lint && npm run typecheck && npm run test",
    "report": "tsx src/bin-report.ts",
    "report:archive": "tsx src/bin-archive.ts",
    "weave": "tsx src/bin-weave.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "eslint": "^9.17.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.18.0",
    "vitest": "^2.1.0"
  }
}
