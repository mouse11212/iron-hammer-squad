# 词灵岛 · 长程任务验证 · 监控观察日志

> 启动 2026-06-25。本日志是「用铁锤小队开发词灵岛」这次**第一个真实长程任务**的监控真相源。
> 监控者职责（人类 BOSS + ③ Compound 层观察者）：① 内循环质量；② 外循环质量；③ 过程是否按既定构思/架构执行；④ 发现的问题记台账；⑤ 优化 = 对 harness（rules/gate/guide/skill/driver）的 diff（§2 反馈固化）。
> 这次验证同时是 NFR（性能/成本/长程可靠性）与 M7 drift sensor 的**真信号采集场**——此前一律 `待长程标定` / `insufficient-data`，从这里开始有真数据。

## 0. 验证对象与三份输入（SoT）

| 输入 | 文件 | 角色 |
|---|---|---|
| PRD | `docs/requirements/词灵岛/词灵岛-PRD与设计交接-v2.md` | 愿景/范围/数据模型/首切片 |
| 原型 | `docs/requirements/词灵岛/wordspirit-prototype-v2.html` | 6 屏交互（login/map/play/chat/shop/番茄钟） |
| 教材 KB | `docs/requirements/词灵岛/教材/`（8 册✅） | 词汇/句型/语法的准确素材 |

- 目标：3–6 年级小学生英语闯关 web 游戏。
- 产物落点：`iron-hammer-output/wordspirit/`（英文命名，遵 CLAUDE.md）。
- 技术栈（PRD §10）：React + Vite + Tailwind + Node/TS + （后续）Postgres；AI 对话经 Agent SDK 注入按年级知识范围。

## 1. 验证方法（怎么"监控开发链路"）

- **从最小垂直切片起步**（§13 / PRD §9）：首切片取三年级最小闭环，先跑通"登录选年级→进一关→听音选词→拿金币→15 分钟落幕"骨架，再按外循环扩。
- **每个 US 过内循环**（driver）：测试 Agent 先写用例(RED) → 开发 Agent TDD(GREEN) → 门禁 → 评审 → squash。我观察每一步质量与是否守纪律。
- **逐 US 记录**：在 §3 内循环台账填一行（规约对齐？测试先行？门禁有效？评审抓到问题？drift？成本/耗时？）。
- **架构符合度**（§4）：对照构思 v4 三层模型 + 8 军规 + 7 红线 + 角色路由逐项核对。
- **问题→优化闭环**（§5/§6）：发现的问题进台账；能固化的产出对 harness 的 diff。

## 2. 验证目标 / 成功判据（据实，不预设阈值）

- **内循环可用且质量好**：能把一个 US 从规约→测试→实现→门禁→评审→可合并跑完，产出可运行且测试有效的代码；门禁能真实拦住坏代码；评审能抓到测试看不到的问题。
- **外循环可用**：能把 PRD 拆成 epic→US→task，按依赖排程，逐 US 推进、bug 能回流修复。
- **过程守架构**：角色不混同、测试先行不被弱化、人类门禁不被绕过、从窄到宽、提交/分支守军规。
- **据实**：NFR（每 US 成本/耗时、长程可靠性）、drift 信号一律记真实测得值，测不到的标 `insufficient-data`，不臆造。

## 3. 内循环质量台账（逐 US）

| 日期 | US | 规约对齐 | 测试先行 | 门禁有效 | 评审有效 | drift | 成本/耗时 | 备注/问题→issue# |
|---|---|---|---|---|---|---|---|---|
| 06-25 | US-1 听音选词判分（attempt1） | ✅ | ✅ | ⏸ 未到（dev phase 崩） | ⏸ 未到 | 未采集 | $0.739，**failed** | 假失败：dev claude -p 瞬时 API 崩 → issue #5 → 修 harness（§6 #1）后重驱 |
| 06-25 | US-1 听音选词判分（attempt2,done） | ✅ 评审第一遍"规约对齐"逐条满足 | ✅ test 先行→dev 实现 | ✅ **有效**：变异门揭"全绿但弱" | ✅ **有效且可量化**：评审抓空options断言弱(2存活变异)→must-fix | fixRounds=1；HIR 0/2；序列待多US | $4.036 / ~18min，**done** | 黄金闭环：评审→must-fix(域=test)→测试Agent回修收紧断言→变异门 93.33%**→100%**→复评过。亲跑 gate 26 测试绿。产物 untracked（legacy 模式不自动 squash，见 §4 待补） |

## 4. 外循环 / 架构符合度台账

| 日期 | 检查项 | 构思/架构依据 | 符合? | 偏差与处置 |
|---|---|---|---|---|
| 06-25 | 内循环角色不混同 | 红线4；V4 角色路由 | ✅ | test/dev/review phase 硬隔离（独立 session）；评审抓到缺陷后 must-fix 域=test，让测试 Agent 回修而非评审自改 |
| 06-25 | 测试先行（RED→GREEN） | 工作节奏 §3；TDD | ✅ | 内循环 test phase 先写用例，dev phase 后实现；重驱时清残留确保 RED 能失败 |
| 06-25 | 确定性 gate 真实拦截 | 三层模型②；门禁 | ✅ | 评审借变异门证据（93.33%，2 存活变异）揪出"绿但弱"的测试缺口——门禁有效性实证 |
| 06-25 | 回修闭环（PEV Verify→回修） | M5 inner-loop | ✅（进行中） | 评审 conditional → must-fix → test-1 回修轮真实触发 |
| 06-25 | **需求澄清角色未被行使** | M2-B 需求澄清；外循环 | ⚠️ **偏差** | US-1 切片为上一会话手工预写，本会话直接驱动；harness 的需求澄清角色（Planner-Workers-Judge）未调用 → **外循环的"需求对齐"目前是隐式手工，非 harness 接管**。处置：作为已知状态记录；PRD 详细+逻辑优先切片降低了即时风险，但完整外循环应补澄清环节（候选优化） |
| 06-25 | **driver 触发=手动，非事件/daemon** | M3 事件驱动 D4；daemon 模式 | ⚠️ **刻意偏差** | 本会话手动 `drive-parallel <db> 1` 单轮 drain，未开 `IH_DAEMON` 无人值守。**刻意**：首次长程验证要在每 US 间留人类观察检查点（红线3 从窄到宽 + 人判质量）。被触发后链路严格守设计（纯状态机+确定性 gate）。处置：daemon 留待 harness 可信后放开（今日假失败缺口印证现在放开会静默烧钱） |
| 06-25 | legacy 模式不自动 squash/集成 | M5-B worktree 隔离+batchIntegrate | ⚠️ 偏差（配方所致） | 用 `drive-parallel`（legacy 就地跑）→ done 产物为 untracked，**不自动 squash 入 git**；交付需手动 commit。`IH_ISOLATION=1` 隔离路径才自动 squash+集成。处置：本验证 BOSS 手动 commit 交付；后续可改用隔离路径走完整交付链（候选） |

## 5. 问题台账（Issue Log）

| # | 日期 | 层级(内/外/架构/harness) | 现象 | 根因(诊断) | 状态 | 处置/优化→§6 |
|---|---|---|---|---|---|---|
| 1 | 06-25 | harness/边界 | driver 不做 greenfield bootstrap，假设 projectDir 已有 git + npm test/lint/typecheck/mutation 脚本 | driver 定位是「内循环 US 编排」，bootstrap 属外循环步骤未实现 | 已认知 | 外循环手工搭骨架（架构正确）；driver 未来可补 bootstrap 能力（候选 diff） |
| 2 | 06-25 | harness | 变异门动态目标正则 `src/*.ts`（排 .test.ts），React 组件 `.tsx` 可能被漏 | `mutateTargetsFromStatus`（gates.ts:50-75）；注：项目 stryker.conf 已含 .tsx（App.tsx 确入变异表），但 driver 运行时用 `--mutate <动态目标>` 覆盖 conf，动态正则才是关键 | 待验证（已窄化） | 收窄到 driver 动态 --mutate；首个 .tsx US 过 driver 时确认；若漏→改正则纳入 .tsx |
| 3 | 06-25 | 内循环/方法 | RED 门要求测试先失败，纯 UI 渲染/样式工作难先写失败测试 | 内循环是 TDD 引擎，最适合有可测逻辑的 US | 已认知 | US 切分把可测逻辑（判分/年级过滤/番茄钟/词库）与纯展示分离，优先驱动逻辑 US |
| 4 | 06-25 | 环境/harness | greenfield `npm install` 撞 `~/.npm/_cacache` root 权限 EACCES | 全局 npm 缓存被 root 文件污染（同项目既知 esbuild 地雷） | 已缓解 | wordspirit 加 `.npmrc`（项目本地 cache）；driver 在该 projectDir 跑 npm 也走本地缓存，已规避 |
| 5 | 06-25 | harness/可靠性 | US-1 dev phase claude -p 进程在 API 重试中途 exit 1、流无 `type:result` 收尾 → 整个 run 判 `failed`，12/12 绿的 scoring.ts 被丢；$0.74 真成本浪费 | 瞬时重试条件 `r.isError && isTransient(r.result)`（`inner-loop-runner.ts:76`）：进程硬崩时 `parsePhaseResult`（`invoke.ts:49`）返回 `result:''`，`isTransient('')=false` → 重试不触发。只接住"模型发出含 overloaded/429 文本的 result"，漏掉"进程没发 result 就死"——而后者是最常见的瞬时基建失败形态 | **诊断完成**（三重证据：dev 流 0 条 result / 末尾 `system/api_retry` / `isTransient('')=false`） | 候选 diff：`parsePhaseResult` 标记 `noResult`，`runFresh`/resume 回退路径对 `isError && noResult` 也重试 → §6。修后再重驱 US-1 端到端验证 |

## 6. 优化台账（对 harness 的 diff · ③ Compound 层产物）

> 每条优化 = 针对某 issue 的根因，固化进 rules/gate/guide/skill/driver 的**具体改动**，使该类问题结构性不可重现（§2）。harness 自身变更须过人类门禁（§5）。

| # | 日期 | 对应 issue# | 优化内容（diff 摘要） | 落点(文件) | 验证 |
|---|---|---|---|---|---|
| 1 | 06-25 | #5 | **transient-retry 盲区根治**：`parsePhaseResult` 给 `PhaseMeta` 加 `noResult`（无 `type:result` 收尾=进程崩溃→true）；`runFresh` 重试条件 `isError && isTransient(result)` 扩为 `isError && (isTransient(result) \|\| noResult)`。严格保留"有 result 的真失败不重试"（回归守护测试）。OpenSpec change `pipeline-phase-crash-retry`（MODIFY 瞬时重试 + ADD noResult 判别），`validate --strict` 通过 | `pipeline/driver/src/invoke.ts`、`inner-loop-runner.ts`；测试 `phase-invoke.test.ts`+`inner-loop-runner.test.ts` | TDD RED→GREEN；driver 门禁 lint/tsc/**254 测试**全绿（+5 新测试）。**变异门口径**：两文件按设计不在静态 mutate 表（IO 邻接，穷尽单测兜底），故不强求 90%；待重驱 US-1 端到端坐实 |

## 7. NFR / drift 真信号采集

> 此前 NFR 性能/成本/可靠性 SLO 与 drift sensor 真信号一律 `待长程标定`/`insufficient-data`。本次长程任务是它们的首个真数据来源。

| 日期 | 指标 | 测得值 | 来源 | 备注 |
|---|---|---|---|---|
| 06-25 | 单 US 成本（failed run） | $0.739 | state.json costUsd | **首个真成本数据**。含 test phase 全程 + dev phase 至崩；本次是假失败浪费 |
| 06-25 | 长程可靠性 | 1/1 run 遇瞬时 API 崩致假失败 | dev-0.jsonl 无 result + `api_retry` | **首个真可靠性信号**：真实 API 抖动率非零；transient-retry 有盲区（issue #5），假失败率被该缺口放大 |
| 06-25 | 单 US 成本（done，含1回修） | $4.036 | state.json（attempt2） | 成功 run：test+dev+review+test回修+复评。**US-1 总成本 = $0.739 + $4.036 = $4.78**（含一次假失败浪费 $0.739） |
| 06-25 | 单 US 耗时（done） | ~18min（22:01→22:19） | run 文件时间戳 + ledger ts | 含一次回修轮 + 两遍评审；单 worker |
| 06-25 | inner-loop 回修率 | fixRounds=1 | verdict.json + state.json | 评审抓"绿但弱"测试 → test回修收紧断言 → 复评过 → done。回修闭环有效 |
| 06-25 | HIR 人工干预率（M7-b） | **0 / 2 run** = 0 | runs-ledger.jsonl 2 行（failed+done，均无 blocked-escalated） | **首个 HIR 真数据**（此前一律 insufficient-data）。2 run 均未升级人类。窗口不足仍未到告警判定 |
| 06-25 | drift（工具序列/语义，M7-a/c） | insufficient-data | 单 US 不足一个一致性窗口 | 需多 US 形成 op 序列/响应序列才有 drift 信号；HIR 已可算 |

---

## 时间线日志（按事件追加）

### 2026-06-25
- 启动长程验证。三份输入就位（教材 KB 8 册已摄取完成）。
- 侦察：输出区将建 `iron-hammer-output/wordspirit/`；driver 驱动方式 = enqueue→drive/drive-parallel→replay；原型 6 屏确认。
- ✅ driver 运行手册到手（Explore）：enqueue→drive/drive-parallel→replay；job=InnerLoopJobSpec。
- ✅ **wordspirit 骨架 bootstrap 完成**（Bootstrap sub-agent）：`iron-hammer-output/wordspirit/` 独立 git 仓库，首提交 `7b31534`；`npm run gate` 全绿（lint+tsc+10 测试），`npm run mutation` 能跑且 stryker.conf 已含 .tsx；注入 24 条三年级 KnowledgePoint 数据种子（上 Welcome/上 U2/下 U1/下 U3）。
- ✅ 驱动前置独立核验：wordspirit gate 我亲跑全绿；claude CLI 2.1.177；driver node_modules 就绪。
- ⏸ **中断点（升级前）**：正准备驱动 US-1，已发出 `claude -p` 冒烟测试（结果未确认）。下面是精确续接说明。

---

## 🔻 续接快照（2026-06-25 升级前 · 升级后的我从这里继续）

### 已完成（不要重做）
1. 教材 KB 8 册全摄取（`docs/requirements/词灵岛/教材/`，README 索引 + 全套总览）。
2. 监控观察日志（本文件）建立：§3 内循环台账 / §4 架构符合度 / §5 问题台账(4 条) / §6 优化台账 / §7 NFR-drift 真信号。
3. driver 运行手册（见下「驱动配方」）。
4. wordspirit 骨架（独立 git 仓库 commit `7b31534`，四门脚本就绪，gate 全绿，数据种子 24 条）。
5. 前置核验全绿（gate / claude CLI / driver）。

### ⏭ 下一步（未完成 —— 从这里开始）
**驱动 US-1「听音选词判分逻辑」过内循环**：
1. （可选）重跑 `claude -p` 冒烟：`timeout 90 claude -p "只回复：就绪" --permission-mode bypassPermissions < /dev/null`。
2. enqueue US-1（见下规约切片）→ drive-parallel 1 worker（后台跑，会 spawn claude -p 子 agent，约 243s+、有真成本）。
3. 观察产物：`pipeline/.runtime/runs/us-wordspirit-001/state.json`（status/fixRounds/costUsd）、`events.jsonl`、wordspirit 里新增的 `src/game/scoring.ts` + 测试。
4. 填 §3 内循环台账（规约对齐/测试先行/门禁有效/评审有效/drift/成本耗时）+ §7 首批 NFR-drift 真信号。
5. 验证问题 #2（driver 动态 --mutate 是否含 .tsx —— US-1 是 .ts，真正验证待 US-5 UI）。
6. 继续 US-2 年级过滤 / US-3 番茄钟900s / US-4 结算 / US-5 接 6 屏 UI。

### US-1 规约切片（enqueue 时贴进 specSlice）
```
能力：听音选词题判分（纯函数，新建 src/game/scoring.ts）
数据类型已存在：src/data/grade3.ts 的 KnowledgePoint。
WHEN 调用 scoreListenPick(question, chosenOptionId)，question={ kpId, options:{id,text}[], correctOptionId }
THEN 返回 { correct, correctOptionId, chosenOptionId }；chosenOptionId===correctOptionId → correct=true，否则 false。
WHEN options 为空，或 options 不含 correctOptionId
THEN 抛 Error（明确信息），不静默返回错误判分。
WHEN chosenOptionId 不在 options 中
THEN 抛 Error（非法选择）。
约束：纯函数无 IO；TypeScript strict；判分只依据入参，不读全局。
```

### 驱动配方（verbatim）
- runtime root：`pipeline/.runtime`；SQLite 队列：`pipeline/.runtime/queue.db`
- 工作目录：`pipeline/driver`，用本地 `npx tsx`（勿用全局 npx，esbuild 缓存地雷）。
- 入队：
  ```
  cd pipeline/driver
  npx tsx src/bin-enqueue-sqlite.ts <repo>/pipeline/.runtime/queue.db us-wordspirit-001 inner-loop '{"specSlice":"<上面切片>","projectDir":"<repo>/iron-hammer-output/wordspirit","maxFixRounds":2}'
  ```
- 驱动（单轮 drain，建议后台 run_in_background）：
  ```
  npx tsx src/drive-parallel.ts <repo>/pipeline/.runtime/queue.db 1
  ```
- 回放：`npx tsx src/bin-replay.ts us-wordspirit-001 <repo>/pipeline/.runtime/events.jsonl`
- 终态 ledger：`docs/metrics/runs-ledger.jsonl` 每个 run 追加一行 → 也是 M7-b HIR 真数据源。

### 监控者职责（升级后保持）
我 = BOSS + ③ Compound 观察者：① 内循环质量；② 外循环质量；③ 过程守构思/架构（三层模型/8 军规/7 红线/角色路由）；④ 问题进 §5 台账；⑤ 根因固化成对 harness 的 diff 进 §6。据实记 NFR/drift 真信号，测不到标 insufficient-data 不臆造。

### 关键事实/地雷（续接必读）
- 三份输入：PRD `词灵岛-PRD与设计交接-v2.md` / 原型 `wordspirit-prototype-v2.html`（6 屏）/ 教材 KB `教材/`（8 册✅）。
- 教材是**外研三起点（孙有中）新版**，非 PRD 写的「一起」（用户已指示本轮只取知识、不动 PRD/原型）。
- 真实语法进阶（据实抽取）：五上=将来时、五下=过去时（先将来后过去，与直觉反）。
- 不 `git add -A`（.stryker-tmp 地雷）；claude -p 须 `< /dev/null` 关 stdin；wordspirit 有项目本地 `.npmrc`（绕 ~/.npm root 权限坑）。
- driver 不做 greenfield bootstrap（外循环职责，问题 #1）；driver 是 TDD 引擎，逻辑优先切 US（问题 #3）。

---

### 2026-06-25（续 · 升级后首次驱动）
- ✅ 续接核验：骨架干净（`7b31534`）、US-1 已在 `requests` 表 queued、claude CLI 2.1.191、driver 就绪。
- ✅ 并行接地：Explore 子 agent 提炼 PRD+原型 6 屏 → US 接地简报（scratchpad），US-2..5 切片方向已定（金币连对/年级过滤/番茄钟状态机/星级评定）。
- 🔴 **驱动 US-1 → `failed`**（state.json，fixRounds=0，$0.739）。**根因诊断完成（issue #5）**：dev 的 claude -p 在一次 API 重试中途硬崩 exit 1、流里 0 条 `type:result`（对照 test 流 1 条）、末尾 `system/api_retry`；`parsePhaseResult` 返回 `result:''` → 瞬时重试 `isTransient('')=false` 不触发 → 假失败。**dev agent 实际已写完 scoring.ts（12/12 绿、typecheck 干净），成果被丢。**
- 📌 这是工程**首个真实长程任务的第一个真信号**：① 真成本/可靠性数据落 §7；② 真实 harness 缺口（transient-retry 盲区）落 issue #5，待固化成 §6 的 harness diff。
- ⏭ **下一步决策点**：修 harness 缺口（`noResult` 也重试，TDD+OpenSpec）→ 重驱 US-1 端到端验证修复；scoring.ts/test 为 untracked 残留，重驱前需清理（否则 RED 测试不失败）。

### 2026-06-25（续 · harness 修复 + US-1 重驱 done）
- ✅ **harness 修复**（issue #5 → §6 优化 #1）：OpenSpec change `pipeline-phase-crash-retry`（MODIFY 瞬时重试 + ADD noResult 判别），TDD RED→GREEN，`PhaseMeta.noResult` + `runFresh` 重试条件扩 `isError && (isTransient(result) || noResult)`，回归守护"真失败不重试"。driver 门禁 lint/tsc/254 测试 + 静态变异门 91.77 全绿。
- ✅ **US-1 重驱 done**（fixRounds=1，$4.036）：保留失败证据（`runs/us-wordspirit-001.failed-attempt1`）→清残留→重置队列→重驱。序列 test→dev→review(conditional)→test-1回修→review-1→done。
- 🏆 **黄金验证链**：评审借变异证据揪"全绿但弱"测试（空options断言只查 toThrow 不查信息内容，2 存活变异）→ must-fix(域=test) → 测试 Agent 收紧断言 → 变异门 **93.33%→100%**(30 killed,0 survived) → 复评过 → done。**门禁有效+评审有效双双可量化坐实**。亲跑 wordspirit gate 26 测试绿。
- 📌 **求真澄清**：本次重驱 **api_retry=0**，noResult 修复**未在生产复现触发**（假失败属非确定性、未复现）→ 修复仅单测验证、待自然复现现场观察，不记战功。意外验证了 resume-fallback（test-1：resume 报错→回退 fresh 成功）。
- 📊 真信号：US-1 总成本 $0.739+$4.036=$4.78（含假失败浪费）；HIR 0/2；ledger 落 2 真行（M7-b 首数据）。
- ⏭ 下一步：US-1 产物 untracked（legacy 模式不 squash），交付待 BOSS 裁定 commit；归档 OpenSpec change；推进 US-2 金币连对加成。
