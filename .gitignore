# ── 依赖 / 构建产物 ──
**/node_modules/
**/dist/
**/build/

# ── 测试/变异工具临时产物 ──
**/.stryker-tmp/
**/reports/mutation/
**/coverage/

# ── 离线工具包(大体积第三方 vendored 内容;按 tools/TOOLS.md 重建) ──
# 仅排除重的 vendored 技能源与依赖;保留 TOOLS.md / install-offline.sh / bin/
/tools/skills/

# ── 机密 / 凭据(绝不入库) ──
*.env
.env
.env.*
*.pem
*.key
*token*
*secret*
.npmrc

# ── 系统 / 编辑器 ──
.DS_Store
*.log
.claude/settings.local.json

# ── OpenSpec 运行缓存(如有) ──
.openspec-cache/

# 流水线运行时状态(队列/run-state,瞬态)
**/.runtime/
# SQLite 队列文件及 WAL/SHM 旁文件(M5-A driver,瞬态运行产物)
**/*.db
**/*.db-wal
**/*.db-shm
